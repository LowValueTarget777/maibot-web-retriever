[project]
name = "maibot-web-retriever"
version = "0.1.0"
description = "MaiBot 网页检索插件 - 支持网页抓取、搜索引擎检索等功能"
requires-python = ">=3.11"
dependencies = [
    "maibot-plugin-sdk>=1.0.0",
    "httpx>=0.27.0",
]
